import java.util.*;

public class Main {
    public static void main(String[] args) {
        // Example input values (you can change these as needed)
        double obtainedMarks = 450;
        double maxMarks = 500;

        double percentage = (obtainedMarks / maxMarks) * 100;
        System.out.printf("Percentage: %.2f%%\n", percentage);
    }
}

